// import AutoLaunch from 'auto-launch';
// import { mainProcessData, handleEmptyPromise } from './util';
// import { app } from 'electron';

export async function enableAutoLaunch() {
    // var autoLauncher = new AutoLaunch({
    //     name: 'Quark'
    // });

    // if (mainProcessData.launchAppOnLogin && app.isPackaged) {
    //     await handleEmptyPromise(autoLauncher.enable());
    // } else {
    //     await handleEmptyPromise(autoLauncher.disable());
    // }

    // autoLauncher.isEnabled()
    //     .then(function (isEnabled) {
    //         if (isEnabled) {
    //             return;
    //         }
    //         autoLauncher.enable();
    //     })
    //     .catch(function (err) {
    //         // handle error
    //         console.error(err);
    //     });
}